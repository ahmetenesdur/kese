/**
 * DAY-1 SPIKE (Gate G1): prove the SDK route end-to-end on Sepolia.
 * Steps: init (docs/strk20-notes.md §2) -> register -> shield small USDC/STRK -> discoverNotes
 *        -> private transfer to a 2nd registered test account -> withdraw -> print timings.
 * Success = all steps pass + proof time measured. Log results to docs/decisions.md (D-004).
 * TODO(claude-code): implement once PROVING_SERVICE_URL is known; until then run with what's available
 * and record exactly where it blocks — that output is the Gate G1 evidence.
 */
console.log("Implement me first — Gate G1 (see PLAN.md Phase S)");
